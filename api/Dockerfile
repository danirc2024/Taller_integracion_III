# Plantilla Base para API Gateway (Ej. FastAPI en Python)
FROM python:3.11-slim

WORKDIR /app

# Descomentar al añadir dependencias
# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt

# COPY . .

# Comando de ejemplo (FastAPI)
CMD ["echo", "API Gateway no implementado aún. Por favor añadir código y descomentar dependencias."]
# CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
