# Plantilla Base para Motor de Rutas y Algoritmia (Espacial)
FROM python:3.11-slim

WORKDIR /app

# Instalar dependencias del sistema si se requieren librerías espaciales como GDAL
# RUN apt-get update && apt-get install -y gdal-bin libgdal-dev

# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt

# COPY . .

# Comando de ejemplo
CMD ["echo", "Motor de optimización espacial no implementado aún. Añadir script de algoritmos."]
# CMD ["python", "spatial_service.py"]
