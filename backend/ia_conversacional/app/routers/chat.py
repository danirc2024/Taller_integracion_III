from fastapi import APIRouter

router = APIRouter()

# TODO: Pegar aquí la lógica de tu función _procesar_chat(proveedor, mensaje)
# y tus 3 endpoints (/chat/groq, /chat/gemini, /chat/nvidia) descritos en la sección 5.9
